<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="lit.amida.checkwork20211127">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Checkwork20211126">
        <activity android:name="lit.amida.checkwork20211127.HomeActivity"/>
        <activity android:name="lit.amida.checkwork20211127.ResultActivity" />
        <activity android:name="lit.amida.checkwork20211127.CountActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>